(() => { "use strict"; /* ========================================= SETTINGS ========================================= */ const ANIMATION_TIME = 1100; const sections = Array.from(document.querySelectorAll(".page-section")); if (sections.length < 2) { return; } /* ========================================= STATE ========================================= */ let currentIndex = 0; let isAnimating = false; let wheelLocked = false; let touchStartY = 0; /* ========================================= PREPARE SECTIONS ========================================= */ sections.forEach((section, index) => { section.dataset.index = index; }); /* ========================================= SLIDE TO SECTION ========================================= */ function goToSection(targetIndex, direction) { if (isAnimating) { return; } if (targetIndex < 0 || targetIndex >= sections.length) { return; } if (targetIndex === currentIndex) { return; } isAnimating = true; const currentSection = sections[currentIndex]; const nextSection = sections[targetIndex]; const currentPanel = currentSection.querySelector(".section-panel"); const nextPanel = nextSection.querySelector(".section-panel"); if (!currentPanel || !nextPanel) { isAnimating = false; return; } /* ===================================== REMOVE OLD STATES ===================================== */ currentSection.classList.remove("enter-bottom", "enter-top", "exit-bottom"); nextSection.classList.remove("enter-bottom", "enter-top"); nextPanel.classList.remove("is-active"); /* ===================================== PREPARE NEXT PANEL ===================================== */ if (direction === "down") { /* * Next section starts below. */ nextSection.classList.add("enter-bottom"); } else { /* * Next section starts above. */ nextSection.classList.add("enter-top"); } /* * Force layout. * * This prevents the browser from * skipping the starting position. */ nextPanel.offsetHeight; /* ===================================== START ANIMATION ===================================== */ requestAnimationFrame(() => { requestAnimationFrame(() => { nextPanel.classList.add("is-active"); }); }); /* ===================================== WAIT FOR ANIMATION ===================================== */ setTimeout(() => { /* * Remove temporary positioning * classes. */ nextSection.classList.remove("enter-bottom", "enter-top"); /* * Keep panel active. */ nextPanel.classList.add("is-active"); /* * Update current section. */ currentIndex = targetIndex; /* * Unlock. */ isAnimating = false; wheelLocked = false; }, ANIMATION_TIME + 50); } /* ========================================= WHEEL ========================================= */ function onWheel(event) { /* * Don't react to tiny wheel movement. */ if (Math.abs(event.deltaY) < 10) { return; } /* * Don't allow another wheel event * during the animation. */ if (isAnimating || wheelLocked) { event.preventDefault(); return; } /* * Only control the page when * we're inside the section stack. */ const direction = event.deltaY > 0 ? "down" : "up"; const targetIndex = direction === "down" ? currentIndex + 1 : currentIndex - 1; /* * At the very beginning/end, * allow normal scrolling. */ if (targetIndex < 0 || targetIndex >= sections.length) { return; } /* * Take control of this wheel * movement. */ event.preventDefault(); wheelLocked = true; goToSection(targetIndex, direction); } /* ========================================= TOUCH START ========================================= */ function onTouchStart(event) { touchStartY = event.touches[0].clientY; } /* ========================================= TOUCH END ========================================= */ function onTouchEnd(event) { if (isAnimating) { return; } const touchEndY = event.changedTouches[0].clientY; const difference = touchStartY - touchEndY; /* * Swipe UP */ if (difference > 50) { goToSection(currentIndex + 1, "down"); } /* * Swipe DOWN */ else if (difference < -50) { goToSection(currentIndex - 1, "up"); } } /* ========================================= KEYBOARD ========================================= */ function onKeyDown(event) { if (isAnimating) { return; } if (event.key === "ArrowDown" || event.key === "PageDown") { event.preventDefault(); goToSection(currentIndex + 1, "down"); } if (event.key === "ArrowUp" || event.key === "PageUp") { event.preventDefault(); goToSection(currentIndex - 1, "up"); } } /* ========================================= EVENTS ========================================= */ window.addEventListener("wheel", onWheel, { passive: false }); window.addEventListener("touchstart", onTouchStart, { passive: true }); window.addEventListener("touchend", onTouchEnd, { passive: true }); window.addEventListener("keydown", onKeyDown); })();